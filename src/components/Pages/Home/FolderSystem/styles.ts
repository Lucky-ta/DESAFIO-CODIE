import styled from "styled-components";

export const FileSystem = styled.div`
    .folders-container {
        display: flex;
        flex-wrap: wrap;
        gap: 1rem;
        padding: 0.6rem;
        padding-top: 1rem;
        border-top: 1px solid grey;
    }
`;

